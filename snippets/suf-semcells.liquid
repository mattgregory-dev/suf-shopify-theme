{% comment %}
  The four scannable cells of an event row: city, date, focus, price.

  Split out because the row renders in two shapes -- a <summary> that opens a
  panel, and a flat <div> for a sold-out or detail-less event -- and the cells
  must be identical in both.

    city    string, required
    date    string, already formatted for display
    focus   string, optional -- blank renders an empty cell
    price   string, already money-formatted
{% endcomment %}

<span class="suf-semrow__city">{{ city | escape }}</span>
<span class="suf-semrow__date">{{ date | escape }}</span>

{%- comment -%}
  THE SPAN IS EMITTED EVEN WHEN EMPTY. The row is a five-column grid, so
  dropping the element would pull the price and the chevron left into the wrong
  columns. An empty grid item still occupies its cell.
{%- endcomment -%}
<span class="suf-semrow__focus">{{ focus | escape }}</span>

<span class="suf-semrow__price">{{ price }}</span>
